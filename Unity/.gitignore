# ═══════════════════════════════════════════════════
# Unity .gitignore
# ═══════════════════════════════════════════════════

# ───────────────────────────────────────────────────
# 용량 큰 자동생성 폴더 (로컬에서 자동 재생성됨)
# ───────────────────────────────────────────────────

# 캐시 — 수 GB 규모, git에 올릴 이유 없음
Library/

# 빌드 임시 파일
Temp/

# WebGL/PC 빌드 결과물 (별도 배포 레포 또는 Vercel로 처리)
Build/
Builds/

# Unity 캐시 (에디터 재시작 시 자동 복원)
obj/
*.csproj
*.sln

# ───────────────────────────────────────────────────
# 로그
# ───────────────────────────────────────────────────
Logs/
*.log

# ───────────────────────────────────────────────────
# 유저별 에디터 설정 (팀원마다 다름)
# ───────────────────────────────────────────────────
UserSettings/

# ───────────────────────────────────────────────────
# 시크릿 — Service Role Key가 담긴 asset
# ───────────────────────────────────────────────────
Assets/Editor/TableGenerator/SupabaseTableConfig.asset
Assets/Editor/TableGenerator/SupabaseTableConfig.asset.meta

# ───────────────────────────────────────────────────
# 메모리 맵 / 크래시 덤프
# ───────────────────────────────────────────────────
*.stackdump
*.crashlytics
sysinfo.txt
MemoryCaptures/
Recordings/

# ───────────────────────────────────────────────────
# supabase-csharp Examples 폴더 (~ 붙여 Unity 제외됨)
# 실제 파일은 있지만 컴파일/추적 불필요
# ───────────────────────────────────────────────────
Assets/Thirdparty/supabase-csharp-master/Examples/BlazorWebAssemblySupabaseTemplate~/
Assets/Thirdparty/supabase-csharp-master/

# ───────────────────────────────────────────────────
# 에셋스토어 패키지 임시 다운로드
# ───────────────────────────────────────────────────
Assets/AssetStoreTools/

# ───────────────────────────────────────────────────
# 올려야 하는 것 (명시적 허용)
# ───────────────────────────────────────────────────
# Assets/           ← 올림 (Scripts, Prefabs, Scenes 등)
# Packages/         ← 올림 (manifest.json, packages-lock.json)
# ProjectSettings/  ← 올림 (빌드 설정, 레이어 등)
